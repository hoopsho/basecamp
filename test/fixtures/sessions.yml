# frozen_string_literal: true

# Session fixtures for controller test authentication.
# Session IDs are strings (generated by SecureRandom.base36 in the model).

admin_session:
  id: 'testsession000000001'
  user: admin_user
  created_at: <%= Time.current %>
  updated_at: <%= Time.current %>
