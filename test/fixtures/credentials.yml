# frozen_string_literal: true

# Credential fixtures — value is encrypted via ActiveRecord::Encryption.
# In test environment, Rails AR encryption still works on fixtures.
# Using `encrypts :value` on the model, the fixture value will be
# transparently encrypted when loaded.

anthropic_api_key:
  service_name: 'anthropic'
  credential_type: 0
  value: 'sk-ant-test-placeholder'
  status: 0
  scopes:
    - 'messages:create'
  created_at: <%= Time.current %>
  updated_at: <%= Time.current %>

slack_api_key:
  service_name: 'slack'
  credential_type: 0
  value: 'xoxb-test-placeholder'
  status: 0
  scopes:
    - 'chat:write'
    - 'reactions:read'
  created_at: <%= Time.current %>
  updated_at: <%= Time.current %>

slack_webhook_secret:
  service_name: 'slack'
  credential_type: 2
  value: 'test-signing-secret'
  status: 2
  scopes: []
  created_at: <%= Time.current %>
  updated_at: <%= Time.current %>

ses_api_key:
  service_name: 'ses'
  credential_type: 0
  value: 'ses-test-placeholder'
  status: 0
  scopes:
    - 'ses:SendEmail'
  created_at: <%= Time.current %>
  updated_at: <%= Time.current %>
